# Задача 1 Написать программу: дан список из 10 чисел, которые задаются с помощью датчика случайных чисел в диапазоне 1-10. 
# Программа находит повторяющиеся элементы и удаляет их из списка. 
# Например, дан список (1,1,1,2,3,4,2,3,4) результат (1,2,3,4)

import random
value = [random.randint(1, 10) for _ in range(10)]
uniq = list(set(value))
print (uniq)

#Солянка из стаковерфлоу и советов
